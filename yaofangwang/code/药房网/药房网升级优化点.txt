优化点：
1、导入窗体label显示文本：第一次加载Excel数据默认只显示10条
2、导入窗体预览的dgv现在列标题都是英文，是否改成中文？
3、所有的操作数据库的，包括增删改查的sql语句，不能用拼接的方式，而应该改成用参数的方式；
4、数据库的增删改操作，可以用datatable来操作，datatable绑定数据库某一张表的名称，然后对datatable
每一列的操作，就对应数据库每一张表下面每一列的操作，最后提交datatable修改内容，也即提交数据库修改
5、所有的操作的弹框提醒，如果能改成界面上label显示，就改成label显示。比如Excel操作，读取啊，导出啊，
上传药房网啊，这个成功与否，包括失败了多少，这些就可以放在界面上，用label来显示
6、后面的导入商品资料的Excel操作，改成用其他Excel读取方式，不用现在的com方式(com已经被微软官方所不推崇了)。
同时，逻辑也稍微修改一下，即一开始选择文件的时候，主要就是为了检查我选择的对不对，所以预览部分只要展示
某一条，或者前几条，确认一下文件是否选对，所以第一次即一次性读完全部Excel文件。至于我是否要报告前台当前总共
要读多少条，过滤掉多少空行、空列，可以保留，这样就不需要定义一个datatable对象来接收我们每次选择的文件，因为
一旦当我选错之后，我是不是还要对这个datatable做清空，重新选择新的文件？意思就是，我们可以不在导入窗体定义一个
datatable来接收我们导入的文件，以防我们选错文件还得考虑对当前datatable做处理。而且第一次只是选择文件，预览只是
给用户确认是否选错文件，因此只需展示前几条记录即可，所以嘛，我们就可以在处理Excel文件那部分方法的时候，只读取到
第几行就可以结束了，并报告前台，当前Excel总共有多少条，过滤掉多少空行、空列，这些信息保留下来，给用户确认，
还是有必要的。后面第二次再点击上传药房网按钮的时候，由于前面咱们没有定义一个datatable对象来接收导入的Excel文件，
因此需要再次打开Excel文件，进行读一条，展示在预览的dgv一条，并上传药房网一条，成功在后面最后一个操作结果记录成功，
失败就记录失败！同时label显示Excel处理的一些信息啊，进度条啊，还是一样展示
7、导入窗体提供一个导出按钮，名为“导入上传失败的记录”，即对上传药房网失败的记录全部导出。该按钮在选择文件的时候
不是编辑，只有当点击上传药房网按钮，全部上传完记录之后，才可操作
8、主界面的两个dgv，需要设置大小，初始化加载的时候即设置大小，同时支持区域大小可调整









================================================================================================
何雨提的关于药房网的优化点：
1、订单丢失，目前是半夜的订单没有同步到我们系统
(通过设置每次，即第一次打开都默认同步上次最后一单对应的最大时间到系统当前时间期间的所有订单来解决)
2、订单状态筛选，过滤掉未付款的订单
（不能直接过滤，订单订购时间是死的，订单状态是一直在变化，但绝对不能一直同步那个时间点，
因为系统一直在往前跑，时间在往后走
可以通过另外一个定时job，定时去调用订单详情接口，根据现有dgv里面的订单，每个去调用订单详情，获取当前
订单新的状态，填充到数据库，并填充到dgv中）
3、在订单主体表格中增加一列“订单时间”（已完成）
4、商品状态的确认，目前是已下架的商品，后边又在药房网上显示上架了
（定时调用SAP接口获取库存等信息，同步药房网个时候，只同步库存、生产日期和有效期，以及批次，不同步是否
下架等状态，也就是说，不从数据库获取其他信息，直接就怼这几个信息给药房网）








